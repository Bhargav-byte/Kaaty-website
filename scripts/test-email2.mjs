import { ConvexHttpClient } from 'convex/browser'; const client = new ConvexHttpClient('https://savory-crab-961.convex.cloud'); const res = await client.action('sendConfirmationEmail:sendConfirmationEmail', { name: 'Test', business: 'Test', phone: '123', email: 'test@example.com', type: 'Test' }); console.log(res);
